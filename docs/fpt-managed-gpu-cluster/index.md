---
id: "fpt-managed-gpu-cluster"
title: "FPT Managed GPU Cluster"
description: "FPT Managed GPU Cluster"
sidebar_label: "FPT Managed GPU Cluster"
sidebar_position: 1
---

# FPT Managed GPU Cluster

**FPT Managed GPU Cluster** is built on the open-source Kubernetes (K8s) platform, which automates the deployment, scaling, and management of containerized applications. FPT Managed GPU Cluster integrates a full set of components — Container Orchestration, Storage, Networking, Security, and PaaS — to provide customers with the best environment for developing and deploying applications on the cloud.

**FPT Managed GPU Cluster** is a managed GPU cluster service model from FKE. With Managed GPU Cluster, FPT Cloud manages all control-plane components while users deploy and manage the Worker Nodes. Managed GPU Cluster lets users focus on application deployment without spending resources on managing the K8s cluster.

**FPT Managed GPU Cluster** is a service model based on the open-source Kubernetes platform that automates the deployment, scaling, and management of containerized applications. In addition to integrating a full set of components — Container Orchestration, Storage, Networking, Security, and PaaS — FPT Managed GPU Cluster also provides GPU resources to support complex compute operations.

Things to be aware of before using **Managed GPU Cluster**:

- **Managed GPU Cluster location**: The geographic region can affect server access speed during use. You should choose the region closest to your traffic sources to optimize performance.
- **Number of nodes and their configurations**: All FPT Cloud accounts are allocated a certain quota for resources such as RAM, CPU, Storage, and IP. Therefore, customers should determine the required resource quantities and maximum limits needed so that FPT Cloud can best support them.

## Home

01 Initial Setup
02 Detailed guides (Tutorials)
