---
id: "db-operation"
title: "データベースの運用"
description: "このセクションでは、FPT Cloud Portal上でのデータベースの起動、停止、再起動、削除などの運用操作について説明します。"
sidebar_label: "データベースの運用"
sidebar_position: 24
---

# データベースの運用

このセクションでは、FPT Cloud Portal上でのデータベースの運用方法について説明します。クラスターのライフサイクル状態に応じて、起動、停止、再起動、削除の操作を実行できます。

### 運用操作

- **Start（起動）**：停止状態のデータベースクラスターを起動します。操作が実行されると、クラスターのステータスは「Starting」に移行し、サービス提供可能になると「Running」になります。
- **Stop（停止）**：実行中のデータベースクラスターを停止します。停止すると、クラスターはワークロードの処理を一時停止します。ステータスは「Stopping」に移行し、その後「Stopped」になります。この状態ではデータベースへのアクセスはできませんが、必要に応じて再起動できます。
- **Restart（再起動）**：実行中のデータベースクラスターを再起動します。この操作はクラスターを停止して再起動するもので、主に設定変更の適用や一時的な問題の解決に使用されます。ステータスは「Restarting」と表示され、完了すると「Running」に戻ります。
- **Delete（削除）**：データベースクラスターとすべての関連データを削除します。この操作は元に戻すことができません。削除が成功すると、クラスターはリストから削除され、関連リソースが解放されます。

### ステータスと利用可能な操作

| ステータス | 意味 | 実行可能な操作 |
| --- | --- | --- |
| Processing | データベースが初期化中です。 | Delete |
| Running | データベースが正常に動作しています。 | Stop、Restart、Delete |
| Stopped | データベースが停止しています。 | Start、Delete |
| Warning | データベースが不安定な状態で動作しています。 | Delete |
| Failed | データベースが不明なエラーに遭遇しています。不要な場合はデータベースを削除してリソースを解放するか、FPTサポートにお問い合わせください。 | Delete |

### 運用操作の実行

操作を実行するには、Database listページを開き、操作対象のデータベースの「Action」列にある「⋯」（More actions）アイコンをクリックします。利用可能なアクションの一覧が表示されます。

[![](/img/migrated/db-list-action-ed761eb4.png)](/img/migrated/db-list-action-ed761eb4.png)

アクションを選択すると、システムは確認ダイアログを表示します。意図しない操作（特に元に戻せない操作）を防ぐため、手動で確認する必要があります。

確認後、システムは約5〜7分でアクションを実行します。この間、プロセスが完了するまでデータベースに対する他の操作は一時的に無効になります。
